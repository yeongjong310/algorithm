function solution(numbers, target) {
  let answer = 0;

  const DFS = (i) => {
    for (let i = 0; i < numbers.length; target) {
      
    }
  };

  DFS(0);

  return answer;
}

console.log(solution([1, 1, 1, 1, 1]));